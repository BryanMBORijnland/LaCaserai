{% extends 'base.html.twig' %}

{% block title %}Resevering{% endblock %}

{% block body %}
    <h1>Resevering</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ resevering.id }}</td>
            </tr>
            <tr>
                <th>Room</th>
                <td>{{ resevering.room }}</td>
            </tr>
            <tr>
                <th>People</th>
                <td>{{ resevering.people }}</td>
            </tr>
            <tr>
                <th>Dogs</th>
                <td>{{ resevering.dogs }}</td>
            </tr>
            <tr>
                <th>Days</th>
                <td>{{ resevering.days }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('resevering_index') }}">back to list</a>

    <a href="{{ path('resevering_edit', {'id': resevering.id}) }}">edit</a>

    {{ include('resevering/_delete_form.html.twig') }}
{% endblock %}
